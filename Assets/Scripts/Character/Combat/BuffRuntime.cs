// Need an interface IBaff so each buff implement a Start / Update / End chronology

// The run time implemenation of each of them explain the logic (tick, ) maybe not for logic since its simple but VFX, sound
